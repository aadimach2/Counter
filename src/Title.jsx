
export const Title=({locked})=>{
    return <h1 className="title">{locked ? ( <span>
        limit Reached Buy <b>Pro</b></span> ):(
            " Fancy Counter"
        )}
   </h1>
}



































































































































